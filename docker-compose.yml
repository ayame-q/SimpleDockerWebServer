version: "3"
services:
  nginx:
    image: nginx:alpine
    env_file:
      - ".env"
    volumes:
      - "./nginx/nginx.conf:/etc/nginx/templates/default.conf.template"
      - "./html:/usr/src/app"
    ports:
      - "$PORT:80"
